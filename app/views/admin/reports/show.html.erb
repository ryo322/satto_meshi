<div class="container">
<<<<<<< HEAD
  <div class="row mt-5">
    <h3 class="page-title">通報詳細</h3>
    <table class="table">
      <tbody>
        <tr>
          <td>通報ID</td>
          <td><%= @report.id %></td>
        </tr>
        <tr>
          <td>通報対象</td>
          <td><%= @report.reported.name %></td>
        </tr>
        <tr>
          <td>理由</td>
          <td><%= @report.reason %></td>
        </tr>
        <tr>
          <td>通報者</td>
          <td><%= @report.reporter.name %></td>
        </tr>
        <tr>
          <td><%= @report.confirmed ? '確認済み' : '未確認' %></td>
          <td>
            <% unless @report.confirmed %>
              <%= link_to '確認する', confirm_admin_report_path(@report), method: :patch %>
            <% end %>
          </td>
        </tr>
      </tbody>
    </table>
  </div>

  <% if @report.reportable_type == '投稿' %>
    <h3><%= link_to "投稿の詳細", post_path(@report.reported) %></h3>
  <% elsif @report.reportable_type == 'コメント' %>
    <h3><%= link_to "コメントの詳細", post_path(@report.reported) %></h3>
  <% end %>
</div>
=======
  <div class="row">
    <h3 class="page-title">通報詳細</h3>
<table class="table">
    <tbody>
      <tr>
        <td>通報ID</td>
        <td><%= @report.id %></td>
      </tr>
      <tr>
        <td>通報対象</td>
        <td><%= @report.reported.name %></td>
      </tr>
      <tr>
        <td>理由</td>
        <td><%= @report.reason %></td>
      </tr>
      <tr>
        <td>通報者</td>
        <td><%= @report.reporter.name %></td>
      </tr>
      <tr>
        <td><%= @report.confirmed ? '確認済み' : '未確認' %></td>
        <td>
          <% unless @report.confirmed %>
            <%= link_to '確認', confirm_admin_report_path(@report), method: :patch %>
          <% end %>
        </td>
      </tr>
    </tbody>
  </table>
  
  <% if @report.reportable_type == '投稿' %>
        <td><%= link_to "投稿の詳細", post_path(@report.reported) %></td>
      <% elsif @report.reportable_type == 'コメント' %>
        <td><%= link_to "コメントの詳細", post_path(@report.reported) %></td>
      <% end %>
</div>
  </div>
</div>

>>>>>>> 5780bfa61166510bab9266abd12c1c258bcfba08
