<h2>いいねした投稿一覧</h2>
<% if @favorited_posts.any? %>
  <ul>
    <% @favorited_posts.each do |post| %>
      <li><%= link_to post.name, post_path(post) %></li>
    <% end %>
  </ul>
<% else %>
  <p>いいねした投稿はありません。</p>
<% end %>
